# Installation aus dem Quellprojekt

```bash
sudo apt install python3 python3-gi gir1.2-gtk-3.0 ffmpeg vlc
python3 src/carbor.py
```

Die spätere DEB- und AppImage-Erstellung wird getrennt getestet. Vorhandene API-Schlüssel und Benutzerdaten liegen weiterhin in den bisherigen XDG-Verzeichnissen.
