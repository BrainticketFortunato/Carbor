# Windows-Paketierung

Noch keine fertige EXE. Dieser Ordner dokumentiert den späteren Port. Eine Windows-Ausgabe muss auf Windows gebaut und getestet werden; ein Linux-Build ist dafür nicht ausreichend.
